## 更新记录

### 版本 1.0.0 - 2024-09-25

- **新增功能**:
  - 串口参数设置界面（波特率、数据位、停止位、奇偶校验）
  - 串口设备自动检测
  - 数据发送和接收日志记录
  - 数据显示格式切换（十六进制/ASCII）

- **修复问题**:
  - 串口发送时的崩溃问题
  - 数据位和停止位设置问题
  - "拒绝访问"错误提示

- **性能优化**:
  - 数据接收和解析速度
  - UI响应速度

### 版本 1.0.1 - 2024-09-26

- **修复问题**:
  - Windows系统串口延迟加载
  - 接收框大数据显示性能

- **改进功能**:
  - 新增帮助文档和FAQ

### 版本 1.0.2 - 2024-09-27 

- **持续问题**:
  - 串口检测功能优化（进行中）

- **新增功能**:
  - 日志文件滚动功能

- **改进功能**:
  - 日志系统优化
    - 添加文件对象缓存
    - 添加了安全的文件关闭机制
    - 线程安全保证

### 版本 1.0.4 - 2024-10-03

- **架构优化**:
  - 改进文件指针生命周期管理
  - 引入智能指针
  - 优化日志滚动逻辑

### 版本 1.0.5 - 2024-10-04

- **新增功能**:
  - windows下串口热插拔检测端口变化情况
  - Linux下轮询检测端口变化情况
  - 串口号自动排序和显示

### 版本 1.0.6 - 2024-10-06

- **性能优化**:
  - 引入线程池管理串口操作
  - UI响应优化

### 版本 1.0.7 - 2024-10-09

- **架构重构**:
  - 串口操作封装(SerialPortManager)
  - 代码模块化优化

### 版本 1.0.8 - 2024-10-12

- **工程改进**:
  - 新增CMake配置模板
  - 优化项目结构

### 版本 1.0.9 - 2024-10-13

- **系统优化**:
  - 日志系统重构
    - 解决文件名更新问题
    - 优化异步写入机制
    - 新增日志管理类和工具类

### 版本 1.1.0 - 2024-10-18

- **系统优化**:
  - 重绘了项目的标题栏和状态栏
  - 添加TitleBar模块
  - 添加StatusBar模块

### 版本 1.1.1 - 2024-11-01

- **架构优化**:(ByteSpace_v5.2)
  - 修改了项目结构
    - 把ByteSpace界面移到Framework模块中
    - 把任务管理部分移植到taskmanager模块中

### 版本 1.1.2 - 2024-11-02

- **修复问题**:
  - 防止 `WndProc` 访问空指针导致崩溃
  - 所有 `Windows API` 操作都增加了错误检查，方便调试
  - 保证注册、注销过程正确进行，防止资源泄漏

### 版本 1.1.3 - 2024-11-05

- **新增功能**:
  - 自定义标题栏
  - 自定义状态栏
  - 定义了一个基类实现了实现无边框窗体拖拽和拉伸
  - 调整了下界面布局

### 版本 1.1.4 - 2024-11-07

- **系统优化**:
  - 将日志的单例模式改为mayers单例
  - 修改了日志只能创建不能写入的问题
  - 修复了日志资源释放问题

### 版本 1.1.5 - 2024-11-09

- **系统优化**:
  - 添加按钮状态管理
  - 添加了错误处理和日志记录
  - 增加日志记录，改进了错误提示信息
  - 串口参数初始化检查
  - 确保所有UI操作都在主线程执行
- **修复问题**:
  - 使用统一的 `Qt::QueuedConnection` 连接信号和槽

### 版本 1.1.6 - 2024-11-11

- **系统优化**:
  - 新增了一个状态管理类，用于管理应用程序的状态转换。
  - 使用单例模式，使用静态实例确保全局唯一
  - 状态转换控制线程安全的实现
  - 状态转换控制
  - 定义了明确的状态转换规则，防止非法的状态转换
  - 使用互斥锁保护状态访问  ，确保多线程环境下的安全性
  - 错误处理
    - 集成错误状态管理
    - 提供错误信息传递机制
  - 信号通知
    - 状态变更通知
    - 错误事件通知
    - 支持错误状态管理
  - 日志集成
    - 记录状态变更，记录错误信息，简单直观的状态查询方法

### 版本 1.1.7 - 2024-11-13

- **新增功能**:
  - 新增了一个配置管理实例, 负责管理应用程序的所有配置项。
  - 单例模式：确保全局只有一个配置管理实例
  - 线程安全：使用互斥锁保护配置访问
  - 自动保存/加载：程序启动时自动加载,退出时自动保存
  - 默认值处理：为所有配置项提供默认值
  - 类型安全：使用具体的 getter/setter 而不是通用接口
  - 变更通知：通过信号通知配置更改
  - 错误处理：提供保存错误的反馈机制

### 版本 1.1.8 - 2024-11-15

- **新增功能**:
  - 添加了配置缓存机制
  - 支持缓存开关控制
  - 提供缓存清理功能
  - 添加事务处理机制
  - 支持批量更新回滚
  - 事务完成通知
  - 错误状态通知
- **系统优化**:
  - 优化读取性能，减少直接文件访问
  - 完善错误处理
  - 详细的错误日志

### 版本 1.1.9 - 2024-11-19

- **系统优化**:
  - 移除了 ByteSpaceTask 对 SerialPortManager 的依赖
  - 简化了基类ByteSpaceTask的构造函数，ByteSpaceTask只负责基本的任务管理，代码职责更明确，保持了基类的通用性
  - SerialPortTask 处理所有串口相关的通用功能，将具体的实现细节移到派生类中，避免基类与具体实现的耦合
  - 具体任务类(如 SerialPortWriteTask)只需关注自己的业务逻辑，更容易扩展新的串口任务类型，代码更易于维护和测试
  - SerialPortWriteTask，SerialPortReadTask，SerialPortOpenTask等类继承自 SerialPortTask 基类，实现对串口的写，读，打开等操作
  
### 版本 1.2.0 - 2024-11-23

- **系统优化**:
  - 处理了代码的一些警告
  - 优化了一些基类构造函数参数冗余问题

### 版本 1.2.1 - 2024-11-26

- **系统优化**:
  - 新增一个 ErrorHandler 类来统一管理错误信息和错误处理逻辑，在 `serialportmanager.cpp` 中的错误处理可以抽象为单独的错误处理类
  - 在 `taskmanager` 中添加任务状态追踪机制,让当前的任务系统可以引入任务队列和任务状态管理
  - 添加配置持久化功能,`serialportconfigurator.h` 可以使用配置文件来管理串口参数
  - 日志系统修复和优化 ：
    - 考虑添加日志过滤功能
    - 修复日志轮转不能写入问题
    - 优化日志文件命名规则
  - 代码结构优化 ：(未完成)
    - 使用依赖注入模式重构 SerialPortManager 类
    - 将一些重复的代码抽象为通用工具类
  - 异步操作优化 ：
    - 在串口操作中引入异步操作机制
    - 使用 Qt 的信号槽机制优化任务执行流程
  - 文档完善 ：
    - 完善代码注释
    - 添加 API 文档

### 版本 1.2.2 - 2024-11-28
- **系统优化**:
  - 添加了边缘检测的容差值，使边缘判断更精确
  - 使用枚举类明确定义了所有可能的调整方向
  - 添加了安全区域概念，比实际边框区域稍大
  - 移除了对 m_areaMovable 的检查，这可能导致光标状态判断不准确
  - 优先检查是否在非边缘区域，如果是则立即设置为箭头光标并返回
  - 引入了最小窗口尺寸限制，防止窗口被拖得太小
  - 优化了拖拽时的几何计算，减少抖动
  - 分离了方向检测和光标更新的逻辑
  - 添加了平滑处理，减少不必要的更新
  - 使用了常量定义关键参数，便于统一管理

### 版本 1.2.3 - 2024-11-30
- **新增功能**:
  - 添加了 `SerialPortReadTask` 类，用于读取串口数据
  - 添加了 `SerialPortWriteTask` 类，用于写入串口数据
  - 添加了 `SerialPortOpenTask` 类，用于打开串口
  - 添加了 `SerialPortCloseTask` 类，用于关闭串口
  - 添加了 `SerialPortConfigTask` 类，用于配置串口参数
- **修复问题**:
  - 修复了串口数据接收时，ByteSpace 类中的 onSerialDataReceived 方法没有被调用的问题。
  - 修复了串口打开时，ByteSpace 类中的 onSerialPortOpened 方法没有被调用的问题。
  - 当串口有数据到达时，SerialPortManager 会发出 dataRead 信号，ByteSpace 类中的 onSerialDataReceived 方法会被调用，将数据显示到接收区。

### 版本 1.2.4 - 2024-12-02
- **修复问题**:
  - 修复了长久以来遗留的一个bug，鼠标光标不能变化和窗口拉伸功能失效的问题。当光标移动到窗口边缘时，光标形状会从箭头变为拉伸形状，但是光标移动到窗口内部时，光标不会恢复为默认光标。
  - 重构了eventFilter函数，使用switch-case结构优化了事件处理逻辑，增加了对鼠标按下、释放和移动事件的处理。
  - 移除了mousePressEvent和mouseMoveEvent中的重复代码，将窗口拉伸相关的逻辑完全交由eventFilter处理，只保留窗口拖动功能。

### 版本 1.2.5 - 2024-12-05
- **新增功能**:
  - 新增了 `StateManager` 类
    - 添加状态历史记录功能：记录状态变化的历史，包括从哪个状态变到哪个状态，时间戳和持续时间
    - 增加状态持续时间统计：跟踪当前状态已经持续了多长时间
    - 添加状态变化回调机制：允许其他组件注册回调函数，在状态变化时得到通知
    - 状态锁定功能：临时锁定状态，防止在关键操作期间发生状态变化
    - 强制状态转换：在特殊情况下可以忽略转换规则强制改变状态
    - 增加状态变化条件检查
    - 添加状态变化锁定/解锁功能
    - 更详细的状态变化拒绝原因

### 版本 1.2.6 - 2024-12-08
- **新增功能**:  
  - 新增了 `ErrorHandler` 类
    - 使用梅耶尔斯单例模式实现，确保全局只有一个错误管理中心
    - 错误分类管理：通过 ErrorType 枚举分类不同来源的错误
    - 错误级别区分：通过 ErrorLevel 枚举区分错误的严重程度
    - 通用错误处理：提供 handleError 方法处理各类错误
    - 专用错误处理：提供 handleSerialPortError 方法处理串口错误
    - 错误状态管理：包括获取最后错误、检查活动错误和清除错误
    - 错误消息映射：预定义各类串口错误的描述信息
    - 信号通知机制：通过信号传递错误信息和清除通知
    - 线程安全设计：确保多线程环境下的安全使用

### 版本 1.2.7 - 2024-12-11
- **系统优化**:  
  - 职责分离 ：StateManager 专注于状态管理，ErrorHandler 专注于错误处理，每个类的职责更加明确，可维护性更好
  - 代码一致性 ：统一使用 Meyers 单例模式
  - 功能完整性 ：错误处理更加完整，包括错误类型、级别等
  - 减少冗余 ：移除了重复的错误处理代码

### 版本 1.2.8 - 2024-12-14
- **系统优化**:  
  - 使 ErrorHandler 成为项目中统一的错误处理中心。
  - 分阶段实施 ：先从核心组件如 SerialPortManager 开始，然后逐步扩展到其他组件。
  - 添加单元测试 ：为 ErrorHandler 添加单元测试，确保它能正确处理各种错误情况。
  - 更新文档 ：在项目文档中添加关于如何使用 ErrorHandler 的说明，确保团队成员了解统一的错误处理方式。
  - 考虑线程安全 ：确保在多线程环境中使用 ErrorHandler 时的线程安全性。
  - 添加错误分析功能 ：可以考虑在 ErrorHandler 中添加错误统计和分析功能，帮助识别常见问题。
  
### 版本 1.2.9 - 2024-12-18

- **系统优化**:  

  1. 优化任务类层次结构
    - 移除了 ByteSpaceTask 的继承关系，这可能导致代码重复和继承关系混乱。
    - SerialPortOpenTask 同时继承自 ByteSpaceTask 和使用 SerialPortTask，这可能导致代码重复和继承关系混乱。
  2. 简化继承层次

```C++
ByteSpaceTask (抽象基类)
├── SerialPortTask (串口任务抽象基类)
│   ├── SerialPortOpenTask
│   ├── SerialPortCloseTask
│   ├── SerialPortReadTask
│   ├── SerialPortWriteTask
│   └── SerialPortConfigTask
└── 其他任务类型
```

### 版本 1.3.0 - 2025-01-02

- **界面优化**:  
  - 把ByteSpace模块拆分为几个子模块
  - 把左侧配置栏，发送区和接收区都拆开成不同模块
  
### 版本 1.3.1 - 2025-01-03

- **界面优化**:  
  - 把左侧配置栏的发送区设置和接收区设置合并为一个设置区域
  - 去掉了冗余的设置，界面更紧凑

### 版本 1.3.2 - 2025-01-04

- **界面优化**:  
  - 把发送区的组件换成QStackedWidget用来切换不同界面
  - 在TitleBar添加了pin按钮

### 版本 1.3.3 - 2025-01-05

- **界面优化**:  
  - 添加传输文件模块
  - 添加CRC校验模块
  - 添加打开文件模块
  - 添加保存文件模块
  - 添加设置模块
  - 调整布局，拉伸的时候不会形变

### 版本 1.3.4 - 2025-01-08

- **界面优化**:  
  - 新增设置界面模块，提供统一的应用程序配置入口
  - 设置界面包含系统、账号/会员、外观、帮助和关于五个分类
  - 系统设置中添加升级配置、网络代理等常规设置选项
  - 账号设置中添加用户登录功能
  - 外观设置中添加主题切换和动画效果控制
  - 帮助模块中整合版本信息、用户指南、常见问题和关于信息
  - 关于模块中添加软件信息、开发团队和法律信息等内容
  - 采用左侧分类列表+右侧内容区的布局设计，界面简洁直观
  - 添加了平滑的页面切换动画效果，提升用户体验
  - 优化了设置项的分类和组织，使配置更加清晰
  - 实现了设置项的自动保存和加载功能

### 版本 1.3.5 - 2025-01-10

- **界面优化**:  
  - 优化了设置界面的分类列表，移除了滑动切换动画效果
  - 移除了指示器，使用背景颜色作为指示器
  - 重构了设置页面的结构，使用ui文件替换代码实现各个界面的组件，大幅减少代码量
  - 将设置界面的各个页面模块化


### 版本 1.3.6 - 2025-01-13

- **界面优化**:  
  - 实现了BytetraceBase和SettingsUI界面的堆叠放置
  - 添加了界面切换功能，通过TitleBar中的按钮可以在BytetraceBase和SettingsUI之间切换
  - 使用QStackedWidget管理多个界面，提高了界面切换的灵活性
  - 优化了界面切换的信号-槽连接机制
  - 完善了资源释放，防止内存泄漏

### 版本 1.3.8 - 2025-01-15

- **新增功能**:  
  - 在 TitleBar 模块中为 labelLogo 添加点击事件，实现与设置按钮相同的功能，点击后可以在 BytetraceBase 和 SettingsUI 界面之间切换。
  - 为 labelLogo 添加鼠标指针样式，在鼠标悬停时显示为手型光标，提示用户该区域可点击。
  - 在 SettingsUI 模块中添加了一个按钮用来返回主界面。
- **功能修复**:
  - 修复了窗口几何尺寸设置问题，添加了 resizeEvent 处理函数
  - 优化了窗口大小调整时的行为，确保界面元素正确响应窗口大小变化
  - 解决了窗口尺寸设置与实际显示不一致的问题
  - 添加了窗口大小变化时的平滑过渡效果

### 版本 1.4.0 - 2025-01-18

- **功能修复**:  
  - 现在设置只能双击TitleBar才能最大化和还原，点击其他模块不能最大化和还原
  - 设置界面的边栏设置窄了一些，由原先的200像素改为现在的150像素


### 版本 1.4.1 - 2025-01-22

- **修改功能**:
  - 把软件名从 `Termnite` 改为 `ByteSpace`
  - 主界面类从 `Termnite` 改为 `ByteSpace`
  - 串口任务类从 `TermniteTask` 改为 `ByteSpaceTask`

### 版本 1.4.2 - 2025-01-25

- **代码优化**:
  - `SerialPortManager`和`SerialPortConfigurator`类功能重叠，两个类都包含了配置串口参数的功能。将所有串口配置功能集中到`SerialPortConfigurator`类中，让`SerialPortManager`只负责调用它。
  - `SerialPortOperator`类与`SerialPortManager`类之间也存在功能重叠，两个类都实现了打开、关闭、读取、写入等基本操作。让`SerialPortOperator`专注于基本的串口操作，而`SerialPortManager`负责更高层次的管理和协调。
  - 信号重复：在`SerialPortManager`类中，`dataReceived`和`dataRead`这两个信号功能几乎相同。
  - `SerialPortManager`类中存在功能相似的方法：`configurePort()`和`configureSerialPort()`以及`configurePort()`。

### 版本 1.4.3 - 2025-01-28
- **代码优化**:
  - 在isInState方法中添加QMutexLocker，确保线程安全
  - 在isInState方法中引入QMutexLocker，防止多线程环境下对m_currentState的并发访问，避免潜在数据竞争。

### 版本 1.4.4 - 2025-01-29
- **代码优化**:
  - 将样式管理功能封装到独立的 `StyleManager` 类中，提高了代码的可复用性、可维护性和清晰度。
  - 新增 `stylemanager.h` 和 `stylemanager.cpp` 文件，定义了 `StyleManager` 类，包含 `loadStyleSheet`、`setupAppStyle` 和 `initAppStyle` 三个静态方法。
  - `StyleManager` 类禁止了实例化、拷贝和赋值操作，确保其作为工具类的纯粹性。
  - `stylemanager.cpp` 实现了这些静态方法，包括加载样式表、设置基础样式以及结合两者初始化完整样式的功能，并增加了日志和参数配置选项。
  - 修改了 `main.cpp` 文件，移除了原有的 `loadStyleSheet` 函数和相关头文件，并用 `StyleManager::initAppStyle(app)` 调用替代了原有的样式设置代码。